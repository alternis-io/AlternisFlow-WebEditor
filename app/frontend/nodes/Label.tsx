import React from "react";
import { NodeProps } from "reactflow";

export function LabelNode(props: LabelNode.Props) {
  return <></>;
}

export namespace LabelNode {
  export interface Props extends NodeProps<{}> {

  }
}
