{
  "name": "dialogue-middleware-workspace",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --no-sort --workspace-concurrency Infinity dev",
    "test": "pnpm -r test",
    "test:e2e": "pnpm -r test:e2e"
  }
}
