{
  "name": "dialogue-middleware-app-workspace",
  "version": "0.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "test:e2e": "pnpm -r test:e2e"
  }
}
